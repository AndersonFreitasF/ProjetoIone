name: easymath
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Dependências do projeto
  firebase_core: ^3.4.1
  firebase_auth: ^5.2.1
  cloud_firestore: ^5.4.1
  provider: ^6.0.3
  url_launcher: ^6.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter

